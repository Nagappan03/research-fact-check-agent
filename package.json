{
  "name": "research-fact-check-agent",
  "version": "1.0.0",
  "main": "research.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node research.js"
  },
  "description": "Single-agent research & fact-check CLI (Claude + web search)",
  "type": "module",
  "dependencies": {
    "@anthropic-ai/sdk": "^0.127.0",
    "dotenv": "^18.0.1",
    "nodemailer": "^10.0.10"
  }
}
